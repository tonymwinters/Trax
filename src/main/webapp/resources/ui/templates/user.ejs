<thead>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Email</th>
    <th>Actions</th>
</thead>
<tbody>
    <% for(var i = 0; i < object.length; ++i){ %>
    <tr>
        <td><%= object[i].firstName %></td>
        <td><%= object[i].lastName %></td>
        <td><%= object[i].contact.emailAddress %></td>
        <td>
            <div class="edit_button"><a href="#">Edit</a></div>
            <div class="delete_button" id="<%= object[i].id %>"><a href="#">Delete</a></div>
        </td>
    </tr>
    <% } %>
</tbody>