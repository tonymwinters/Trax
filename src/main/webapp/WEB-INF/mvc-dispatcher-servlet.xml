<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">


    <context:component-scan base-package="com.trax"/>

    <context:annotation-config/>

    <!-- Override the local properties with /etc/translation file on the server -->
    <context:property-placeholder location="classpath:configs/config.properties, file:/etc/trax/config.properties" ignore-unresolvable="true" ignore-resource-not-found="true"/>

    <bean name="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="driverClassName" value="${DBDriver}" />
        <property name="url" value="${DBHost}" />
        <property name="username" value="${DBUsername}" />
        <property name="password" value="${DBPassword}" />
    </bean>

</beans>